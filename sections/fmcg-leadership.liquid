{%- style -%}
  {{ section.settings.name_font | font_face: font_display: 'swap' }}
  {{ section.settings.role_font | font_face: font_display: 'swap' }}

  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
    background-color: {{ section.settings.bg_color }};
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .leadership-grid-{{ section.id }} {
    display: grid;
    grid-template-columns: 1fr;
    gap: 30px;
    max-width: var(--page-width, 1200px);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  @media screen and (min-width: 750px) {
    .leadership-grid-{{ section.id }} {
      grid-template-columns: repeat(2, 1fr);
      column-gap: 40px;
      row-gap: 60px;
    }
  }

  @media screen and (min-width: 990px) {
    .leadership-grid-{{ section.id }} {
      grid-template-columns: repeat({{ section.settings.columns_desktop }}, 1fr);
    }
  }

  .leadership-profile-{{ section.id }} {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    transition: transform 0.3s ease;
    height: 100%;
    align-items: flex-start;
    text-align: left;
  }

  .leadership-profile-{{ section.id }}:hover {
    transform: translateY(-5px);
  }

  .leadership-profile__image-wrap-{{ section.id }} {
    background-color: {{ section.settings.image_bg_color }};
    border-radius: {{ section.settings.image_radius }}px;
    overflow: hidden;
    margin-bottom: 24px;
    aspect-ratio: 1.1 / 1;
    position: relative;
    width: 100%;
    display: flex;
    align-items: flex-end;
    justify-content: center;
  }

  .leadership-profile__image-wrap-{{ section.id }} img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.5s ease;
  }

  .leadership-profile-{{ section.id }}:hover .leadership-profile__image-wrap-{{ section.id }} img {
    transform: scale(1.03);
  }

  .leadership-profile__name-{{ section.id }} {
    font-family: {{ section.settings.name_font.family }}, {{ section.settings.name_font.fallback_families }};
    font-size: {{ section.settings.name_size_mobile }}px;
    color: {{ section.settings.name_color }};
    font-weight: {{ section.settings.name_weight }};
    margin: 0 0 10px 0;
    line-height: 1.2;
  }

  @media screen and (min-width: 750px) {
    .leadership-profile__name-{{ section.id }} {
      font-size: {{ section.settings.name_size }}px;
    }
  }

  .leadership-profile__role-{{ section.id }} {
    font-family: {{ section.settings.role_font.family }}, {{ section.settings.role_font.fallback_families }};
    font-size: {{ section.settings.role_size_mobile }}px;
    color: {{ section.settings.role_color }};
    font-weight: {{ section.settings.role_weight }};
    margin: 0;
    line-height: 1.5;
  }

  @media screen and (min-width: 750px) {
    .leadership-profile__role-{{ section.id }} {
      font-size: {{ section.settings.role_size }}px;
    }
  }
{%- endstyle -%}

<div class="section-{{ section.id }}-padding">
  <div class="leadership-grid-{{ section.id }}">
    {%- for block in section.blocks -%}
      {%- if block.settings.link != blank -%}
        <a href="{{ block.settings.link }}" class="leadership-profile-{{ section.id }}" {{ block.shopify_attributes }}>
      {%- else -%}
        <div class="leadership-profile-{{ section.id }}" {{ block.shopify_attributes }}>
      {%- endif -%}

        <div class="leadership-profile__image-wrap-{{ section.id }}">
          {%- if block.settings.image != blank -%}
            {{ block.settings.image | image_url: width: 800 | image_tag: loading: 'lazy' }}
          {%- else -%}
            {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
          {%- endif -%}
        </div>
        
        <h3 class="leadership-profile__name-{{ section.id }}">{{ block.settings.name }}</h3>
        <p class="leadership-profile__role-{{ section.id }}">{{ block.settings.role }}</p>

      {%- if block.settings.link != blank -%}
        </a>
      {%- else -%}
        </div>
      {%- endif -%}
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "FMCG Leadership",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Section Layout"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3,
      "label": "Columns on desktop"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "image_bg_color",
      "label": "Image Wrapper Background",
      "default": "#e2ceb9"
    },
    {
      "type": "range",
      "id": "image_radius",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Image Border Radius",
      "default": 20
    },
    {
      "type": "header",
      "content": "Name Typography"
    },
    {
      "type": "font_picker",
      "id": "name_font",
      "label": "Name Font",
      "default": "assistant_n7"
    },
    {
      "type": "range",
      "id": "name_size",
      "min": 12,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Name Font Size (Desktop)",
      "default": 24
    },
    {
      "type": "range",
      "id": "name_size_mobile",
      "min": 12,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Name Font Size (Mobile)",
      "default": 20
    },
    {
      "type": "color",
      "id": "name_color",
      "label": "Name Color",
      "default": "#003a70"
    },
    {
      "type": "select",
      "id": "name_weight",
      "label": "Name Font Weight",
      "options": [
        { "value": "400", "label": "Normal (400)" },
        { "value": "500", "label": "Medium (500)" },
        { "value": "600", "label": "Semi Bold (600)" },
        { "value": "700", "label": "Bold (700)" },
        { "value": "800", "label": "Extra Bold (800)" },
        { "value": "900", "label": "Black (900)" }
      ],
      "default": "700"
    },
    {
      "type": "header",
      "content": "Role Typography"
    },
    {
      "type": "font_picker",
      "id": "role_font",
      "label": "Role Font",
      "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "role_size",
      "min": 10,
      "max": 36,
      "step": 1,
      "unit": "px",
      "label": "Role Font Size (Desktop)",
      "default": 16
    },
    {
      "type": "range",
      "id": "role_size_mobile",
      "min": 10,
      "max": 36,
      "step": 1,
      "unit": "px",
      "label": "Role Font Size (Mobile)",
      "default": 14
    },
    {
      "type": "color",
      "id": "role_color",
      "label": "Role Color",
      "default": "#666666"
    },
    {
      "type": "select",
      "id": "role_weight",
      "label": "Role Font Weight",
      "options": [
        { "value": "300", "label": "Light (300)" },
        { "value": "400", "label": "Normal (400)" },
        { "value": "500", "label": "Medium (500)" },
        { "value": "600", "label": "Semi Bold (600)" },
        { "value": "700", "label": "Bold (700)" }
      ],
      "default": "400"
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "profile",
      "name": "Profile",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Profile Image (Transparent PNG recommended)"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Leader Name"
        },
        {
          "type": "textarea",
          "id": "role",
          "label": "Role",
          "default": "Chief Executive Officer"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Profile Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FMCG Leadership",
      "blocks": [
        {
          "type": "profile",
          "settings": {
            "name": "Ramon L. Laguarta",
            "role": "Chairman and Chief Executive Officer"
          }
        },
        {
          "type": "profile",
          "settings": {
            "name": "Anne Tse",
            "role": "Chief Executive Officer, Asia Pacific Foods"
          }
        },
        {
          "type": "profile",
          "settings": {
            "name": "Athina Kanioura",
            "role": "Chief Executive Officer, Latin America Foods\nand Executive Vice President, Chief Strategy\n& Transformation Officer"
          }
        }
      ]
    }
  ]
}
{% endschema %}
